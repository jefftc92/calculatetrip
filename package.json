{
  "name": "calculatetrip",
  "version": "1.0.0",
  "description": "Static all-inclusive resort guide. Pure Node.js + EJS — pre-rendered HTML, zero client framework, SEO-first.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node serve.js",
    "serve": "node serve.js"
  },
  "dependencies": {
    "ejs": "3.1.10"
  },
  "devDependencies": {
    "tailwindcss": "3.4.13"
  }
}
